<div id="signin_section">
  <div class="signin_box">
    <p>Logged as <%= "#{@user.first_name} #{@user.last_name}" -%></p>
    <p><%= link_to t('layout.sidebar.user_menu.user_home'), :controller => 'user', :action => 'home' -%></p>
    <p><%= link_to t('layout.sidebar.user_menu.user_addresses'), :controller => 'user', :action => 'addresses' -%></p>
    <p><%= link_to t('layout.sidebar.user_menu.logout_button'), :controller => 'user', :action => 'logout' -%></p>
  </div>
</div>
